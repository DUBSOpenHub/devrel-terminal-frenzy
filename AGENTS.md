# 🎮 DevRel Terminal Frenzy — Agent Architecture

## Overview

DevRel Terminal Frenzy is a single-file HTML5 arcade game built with AI agents via GitHub Copilot CLI. The entire game — HTML, CSS, JavaScript, sound design — was generated by AI and refined through human-guided iteration.

## Architecture

```
┌─────────────────────┐
│   Human Orchestrator │
│   (Copilot CLI)      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   AI Code Generation │
│   (general-purpose   │
│    agents)           │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   Single HTML File   │
│   index.html         │
│   ~2,000 lines       │
└─────────────────────┘
```

## Key Design Decisions

| Decision | Rationale |
|----------|-----------|
| Single HTML file | Zero friction — double-click to play, host anywhere |
| Web Audio synthesis | No external audio files needed |
| GitHub avatar CDN | Always up-to-date team photos, CORS-enabled |
| localStorage only | No server, no accounts, no data collection |
| 8-key KEYSET (A,S,D,F,J,K,L,;) | Home row keys for touch-typing ergonomics |
| Wrong-key penalty (5% score) | Prevents button-mashing exploit |

## Game Components

- **Title Screen** — 6-line typing animation with team faces, gradient orbs, frosted glass instruction card
- **Play Screen** — 4×3 card grid, pipeline combo meter, timer, urgent request overlays
- **Game Over** — Score, rank, stats, victory jingle
- **Audio** — All synthesized via Web Audio API (beeps, buzzer, victory fanfare)
- **Visuals** — Canvas matrix rain, CSS particles, gradient orbs, screen shake

## Data

- **96 team members** in the `DEVREL` array (GitHub username + avatar URL)
- **96 typing phrases** in `TYPING_PHRASES_ALL` (one per person, shuffled on load)
- **20 PR title jokes** in `PR_TITLES`
- **6 urgent request commands** in `BOSS_COMMANDS`
- **4 power-up types** in `POWERUPS`
